# spotter-ai-fullstack-assessment
A full-stack web app built with Django and React that generates trip routes and simulates ELD logs for drivers. It takes locations and cycle hours, then outputs route maps, stops, rest breaks, and automated daily log sheets using driving rule assumptions with a clean UI.
